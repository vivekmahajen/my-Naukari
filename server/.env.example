# Copy to .env and adjust for your environment.
DATABASE_URL=postgresql://naukri:naukri_dev_pw@localhost:5432/naukriplus
JWT_SECRET=change-me-to-a-long-random-string
# Optional: enables the browser seed endpoint /api/admin/seed?key=…
# Leave unset to keep that endpoint disabled (recommended once seeded).
SEED_KEY=
PORT=4000
# Comma-separated origins allowed to call the API (frontend origin)
CORS_ORIGIN=http://localhost:8000
